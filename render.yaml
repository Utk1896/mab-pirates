# render.yaml — MAB Pirates deployment blueprint

services:
  - type: web
    name: mab-pirates
    runtime: docker
    dockerfilePath: ./Dockerfile
    region: singapore
    plan: free
    envVars:
      - key: MENTOR_PASSWORD
        value: pirates2025
